import styled from 'styled-components'

// Solo reset de botón — lo visual es components/Pokeball
export const Button = styled.button`
  display: inline-flex;
  flex-shrink: 0;
  padding: 0;
  border: none;
  background: none;
`
